<nav class="flex justify-between my-4 mr-8">
  <div class="ml-10">
    <a href="<%= relative_url "/" %>">
    <%= svg "/images/logo.svg", class: "#{hide_svg} w-[321px] h-[32px] text-black" %>
      </a>
  </div>
  <div class="<%= text_color %> flex justify-end space-x-4 items-center">
    <div><a href="<%= relative_url '/about' %>">About</a></div>
    <div><a href="<%= relative_url '/eu-acquis' %>">EU Acquis</a></div>
    <div class="relative">
      <div data-controller="dropdown" class="">
        <button data-dropdown-target="trigger" type="button"
            class="inline-flex items-center justify-center whitespace-nowrap"
        >
        <span class="mr-2">Beneficiaries</span>
        <%= svg '/images/menu_arrow.svg' %>
      </button>
      <div data-dropdown-target="container"
          id="beneficiaries-menu"
          class="absolute left-0 z-40 w-screen max-w-sm hidden px-2 mt-3 sm:px-0"i
      >
        <div class="overflow-hidden rounded-lg shadow-lg ring-1 ring-black ring-opacity-5">
          <div class="relative grid gap-6 px-5 py-6 bg-white sm:gap-8 sm:p-8">
            <div class="block p-3 -m-3">
              <ul class="space-y-8 text-sm">
                <% dato.beneficiaries.each do |b| %>
                  <li>
                    <a href="<%= relative_url b.slug %>"><%= b.title %></a>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      </div>
      </div>
    </div>
    <div><a href="<%= relative_url '/updates' %>">Updates</a></div>
    <div><a href="<%= relative_url '/opportunities' %>">Opportunites</a></div>
    <div class="text-gray-400">|</div>
    <div><a href="https://k4ra.admin.datocms.com" class="<%= button_color %> px-4 py-2 rounded">Login</a>
  </div>
</nav>
