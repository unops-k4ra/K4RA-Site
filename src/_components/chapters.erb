<div class="grid max-w-screen-lg grid-cols-2 gap-6 px-4 mx-auto sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 sm:px-6">
  <% chapters.each_with_index do |chapter, index| %>
    <%= render Chapter.new(resource: chapter, options: {index: index}) %>
  <% end %>
</div>
