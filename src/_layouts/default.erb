<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: resource.data.title %>
  </head>
  <body class="">
    <div class="mx-auto max-w-screen-default">
      <%= yield %>
      <%= render "footer", metadata: site.metadata %>
    </div>
  </body>
</html>
